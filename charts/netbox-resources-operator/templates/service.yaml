apiVersion: v1
kind: Service
metadata:
  name: {{ include "netbox-resources-operator.fullname" . }}
  labels:
    {{- include "netbox-resources-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "netbox-resources-operator.selectorLabels" . | nindent 4 }}
